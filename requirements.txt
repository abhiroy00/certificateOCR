# core
openai>=1.30.0
flask>=3.1.0
waitress>=3.0.0
# Stores the API key in Windows Credential Manager / macOS Keychain /
# Secret Service. Optional but strongly recommended - without it the key
# falls back to an obfuscated 0600 file in the work folder.
keyring>=24.0.0
pillow>=10.0.0
python-dotenv>=1.0.0

# PDF support - either works, PyMuPDF is the easier one to get running:
#   pymupdf    : pure pip wheel, no extra install, works out of the box
#   pdf2image  : needs the poppler binary on PATH too (see README)
pymupdf>=1.24.0
pdf2image>=1.17.0

# optional offline engine
pytesseract>=0.3.10

# optional drag & drop in the Tkinter window
tkinterdnd2>=0.4.2

# optional: powers "Download Excel" / `cli export-xlsx` - a one-shot .xlsx
# with real (blue, underlined) hyperlinks, which plain CSV cannot carry.
# Not needed for the primary sharded-CSV output.
openpyxl>=3.1.0
